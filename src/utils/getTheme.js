export const getTheme = (theme) => {
  if (theme === "red") return "text-red-600 border-red-600";
  if (theme === "orange") return "text-orange-400 border-orange-400";
  if (theme === "green") return "text-green-600 border-green-600";
    if (theme === "white") return "text-white border-white";
};
